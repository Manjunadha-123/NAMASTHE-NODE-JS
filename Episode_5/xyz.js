// All code in the module is wrapped in file
// require('./path); comming from Node.js
// module.exports = {} comming from Node.js
// IIFE-Immediate Invoked Function Expression
(function () {
    // IIFE-Immediate Invoked Function Expression
    // This is a self-executing function that runs as soon as it is defined.
    console.log('I am an IIFE!');
    // The code inside this function runs immediately and does not pollute the global scope.
    // It is often used to create a private scope for variables and functions.

    // it is mainly provide privacy of  variables and functions.
})()
